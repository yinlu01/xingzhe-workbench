# 行者工作台 · 项目交接文档

> 版本：v1.0 ｜ 更新：2026-08-26 ｜ 维护人：yinlu01（果果）
>
> 本文档面向**后续接手维护的人（或未来的自己）**，重点交代：项目怎么跑、文件在哪儿、数据存在哪、**核心机制如何运转**、有哪些坑。功能细节见 `README.md`，历史改动见 `CHANGELOG.md`，规划模块设计背景见 `设计文档-时间规划模块.md`。

---

## 一、项目是什么

**行者工作台** = 个人生活与学习工作台（读书 / 运动 / 人脉 / 待办 / 复盘 / 规划），一个界面全搞定。

- **本地优先**：单文件前端（PWA）+ Python 本地服务 + macOS 桌面壳（Swift + WKWebView），数据全在本机，不上云。
- **三种访问方式互通**：桌面 App（`行者工作台.app`）、浏览器、手机（同一局域网）。

**运行方式**（详见 `README.md`）：
- 桌面 App 打开即用，会自动拉起 `server.py`（端口 8765）。
- 浏览器 / 手机：`./start.sh` 后访问 `http://localhost:8765`（或局域网 IP）。

---

## 二、文件地图（改东西先看这张表）

| 文件 | 职责 | 备注 |
|---|---|---|
| `mac-dashboard.html` | **主文件**：全部 UI + JS（约 3900 行，单文件内联） | 绝大多数改动都在这里 |
| `server.py` | 本地静态服务 + JSON API + Web Push | 端口 8765 |
| `sw.js` | Service Worker（网络优先 + Push） | PWA 离线 |
| `manifest.json` | PWA 清单 | |
| `macos_app/main.swift` | Swift 桌面壳（WKWebView + 通知桥接） | 改壳才动这里 |
| `agent_study_plan.json` | Agent 学习计划（12 周 84 天，5 阶段） | 由脚本生成 |
| `build_agent_plan.py` | 生成 `agent_study_plan.json` | |
| `reading_plan.json` | 读书计划（按日期推进） | 个人数据，已 gitignore |
| `update_today.py` | 读书计划按真实日期推进 | |
| `review.json` | 每日复盘 + AI 分析 | 个人数据，已 gitignore |
| `weread_sync.py` / `weread_data.json` | 微信读书同步（含近期笔记正文 `recentNotes`） | |
| `weread_summary.json` | AI 读书笔记总结（每日自动化生成） | 个人数据，gitignore |
| `obsidian_scanner.py` / `obsidian_report.json` | Obsidian 知识库扫描 | |
| `sync_obsidian_review.py` | 复盘归档到 Obsidian（日/周） | |
| `notify.py` | 命令行发推送通知 | |
| `CHANGELOG.md` | 更新日志（**每次改动都要记**） | |
| `README.md` | 功能总览 + 技术架构 + 部署 | |
| `设计文档-时间规划模块.md` | 规划模块设计稿（已完成编码，供追溯） | |

> ⚠️ 个人数据文件（`review.json` / `reading_plan.json` / `weread_data.json` / `weread_summary.json` / `obsidian_report.json` / `life-dashboard.html`）已 gitignore，不会进 git。

---

## 三、数据存储全景（交接关键）

### 3.1 浏览器端 → `localStorage`（前缀 `wb_life_`）

封装函数：`ld()/sv()`（数组）、`lg()/sg()`（对象）、`ts()`（今天日期字符串 `YYYY-MM-DD`）。

| key | 含义 |
|---|---|
| `wb_life_goals` | 核心目标 |
| `wb_life_tasks` | 待办清单 |
| `wb_life_exercises` | 运动打卡记录 |
| `wb_life_weights` | 体重记录 |
| `wb_life_books` | 在读书目 |
| `wb_life_notes` | 读书笔记 |
| `wb_life_study_checkin` | 学习打卡（热力图数据） |
| `wb_life_contacts` | 联系人 |
| `wb_life_comm_logs` / `wb_life_visit_logs` | 联系 / 拜访记录 |
| `wb_life_obsidian` | Obsidian 报告缓存 |
| `wb_life_weread` | 微信读书数据 |
| `wb_life_plan` | 读书计划缓存（reading_plan.json 同步） |
| `wb_life_plan_ck` | 读书打卡（`planCheckins`） |
| `wb_life_agent_plan` | Agent 学习计划缓存 |
| `wb_life_agent_ck` | Agent 学习打卡 |
| `wb_life_plan_daily` | **每日计划（含顺延任务）** ← 规划核心数据 |
| `wb_life_plan_weekly` | 周计划 |
| `wb_life_notify_last` | 通知去重 |
| `wb_life_rv_ai_notified` | 复盘 AI 分析通知去重 |
| `wb_life_initialized` / `wb_life_agent_mig` / `wb_life_contact_mig` 等 | 一次性迁移标记 |

### 3.2 服务端 → JSON 文件

`review.json`（复盘，结构见 README）、`reading_plan.json`、`agent_study_plan.json`、`weread_data.json`、`weread_summary.json`（AI 读书笔记总结，结构 `{date, generatedAt, summary, highlights}`）、`obsidian_report.json`。

> 关键：`wb_life_plan_daily` 是**规划/顺延的单一事实源**，其他展示都从这里读（见下节）。

---

## 四、核心机制详解（本次重点改造）

### 4.1 任务顺延机制（Rollover）

**要解决的问题**：前一天没完成的「读书 / 学习」任务，不能被跳过，要顺延到第二天，做完旧的才推进新内容。

**数据流**：

```
数据源 JSON（异步加载）
      ↓
ensurePlanForDate(今天)          ← 每天生成晚间例行计划
      ↓
planRolloverFrom(昨天)           ← 查昨天晚间未完成的 reading/study
      ├─ 有欠账 → 顺延旧任务（打 rolled 标记，不生成今日新任务）
      └─ 无欠账 → 生成今日新章节
      ↓
wb_life_plan_daily[今天]          ← 落库
      ↓
getTodayDailyTasks()             ← 全站统一出口
      ↓
首页 / 规划页 / 读书页 / 任务页   ← 一致展示
```

**关键规则**：
- **只顺延 `reading` 和 `study`**；`exercise`/`review`/`ai` 是每日例行，不顺延。
- **顺延优先**：有欠账时，不再生成当天同类型的新任务，先补旧账。
- 顺延任务带 `rolled: true` 和 `rolledFrom: 'YYYY-MM-DD'`，detail 追加「未完成 · 自 X/X 顺延」。
- 跨月 / 跨年由 `planPrevDate()`（用 Date 对象倒退一天）保证安全。
- 休息日边界：agent 计划含 12 个周六 `rest` 日，休息日不生成新学习任务，但欠账仍会顺延。

### 4.2 全站一致性（单一数据源）

**所有「今日该做什么」的展示，统一由 `getTodayDailyTasks()` 驱动**，不再各自读 `reading_plan.json` / `agent_study_plan.json` 的日期推进数据。

7 处展示入口（已全部接入）：

| 入口 | 位置 |
|---|---|
| 首页 · 今日概览 banner | `renderHome()` |
| 首页 · 读书卡 | `renderReadingTaskCards()` |
| 首页 · Agent 学习卡 | `renderAgentCards()` |
| 首页 · mini 计划卡 | `renderHomePlan()` |
| 任务页 · 读书卡 | `renderReadingTaskCards()` |
| 读书页 · hero | `renderReadingPlan()` |
| 规划页 · 日/周月/待办 | `renderPlanDay()` 等 |

`getTodayPlanInfo()` / `getTodayAgentInfo()` 现在只做两件事：**作为数据源给生成逻辑用**、**作为首页 fallback 兜底**。

### 4.3 缓存失效「双兜底」（为什么重启就生效）

**背景坑**：`ensurePlanForDate` 生成后打 `generated:true` 永久缓存；如果生成时数据还没到位，或代码升级后，缓存不会自动重算 → 出现「改了代码没反应」。

**两道兜底**：
1. **`PLAN_SCHEMA` 版本号**（当前 = 2）：`ensurePlanForDate` 发现 `daily[date].v !== PLAN_SCHEMA` 就强制重算。**改了顺延/生成逻辑，记得把 `PLAN_SCHEMA` +1**，这样用户重启 App 即生效。
2. **`planRefreshToday()`**：挂在 `loadReadingPlan()` / `loadAgentPlan()` 成功回调上，数据异步就绪后主动让今日计划失效重算。注意它只「失效标记」（`generated=false` + 删 `v`），**不整条 delete**，以保留用户上午/下午自定义任务。

### 4.4 打卡双向同步

`planSyncTodayFromCheckins()`：以 `wb_life_plan_ck`（读书）/ `wb_life_agent_ck`（学习）为准，双向同步每日计划任务的 `done` 状态。打卡入口（`checkPlanToday` / `togglePlanCheckIn` / 微信读书自动打卡）标记后会触发 `renderHome()` 即时刷新。

### 4.5 最近读书笔记（微信读书 + AI 总结）

首页「最近读书笔记」由 `renderHomeNotes()` 驱动，两层：

1. **数据源**：`weread_sync.py` 的 `fetch_recent_notes()` 拉取微信读书近期划线（`/book/bookmarklist`）与想法（`/review/list/mine`）正文，产出 `weread_data.json` 的 `recentNotes` 字段。
2. **AI 总结**：`weread_summary.json`（`{date, generatedAt, summary, highlights}`）由 WorkBuddy 自动化「每日读书笔记总结」（每日 22:30）生成；前端轮询加载到 `wb_life_weread_summary`，展示在首页「AI 读书笔记总结」区块。

---

## 五、维护手册（常见操作与坑）

### 5.0 定时自动化清单

| 时间 | 任务 | 说明 |
|---|---|---|
| 22:30 | 每日读书笔记总结 | 扫描 weread_data.json 近 3 天笔记，生成总结写回 weread_summary.json |
| 23:00 | 每日复盘分析 | 分析近 7 天复盘生成 AI 分析（详见 README） |

### 5.1 改完前端怎么生效

- **浏览器 / PWA**：`server.py` 对 `.html/.json/.svg` 返回 `Cache-Control: no-store`，刷新即可。
- **桌面 App**：改 `mac-dashboard.html` 后**需重启「行者工作台」App**（WKWebView 不自动重载）。

### 5.2 改了顺延/生成逻辑必做

1. `PLAN_SCHEMA` +1（否则用户重启也不重算）。
2. 更新 `CHANGELOG.md`。
3. 本地起 `server.py` 用浏览器实测（见 5.4 的验证思路）。

### 5.3 git 提交（坑：失效代理）

若 `git push` 报 `502` / `Empty reply from server` / `CONNECT tunnel failed`，多半是环境变量 `HTTP_PROXY/HTTPS_PROXY` 指向了已失效的本地代理。**去掉代理直连**：

```bash
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy -u ALL_PROXY -u all_proxy git push
```

先诊断：`curl --noproxy '*' -o /dev/null -w "%{http_code}" https://github.com`（返回 200 说明直连通）。

### 5.3b git 提交（坑：残留锁文件）

本仓库锁文件问题高发（2026-09-04 一天内踩了 4 次）。两类锁，症状都是 `Another git process seems to be running`：

| 锁文件 | 触发原因 | 处理 |
|---|---|---|
| `.git/index.lock` | git 写索引过程中被中断 | 确认无 git 进程后 `rm -f` |
| `.git/refs/remotes/origin/main.lock` | push 更新远程引用时被中断 | 同上 |

**根因（关键）**：用 `git ... \| head -n` 这类管道时，`head` 读够行数就关闭管道，git 收到 **SIGPIPE 被杀**，来不及删除自己创建的锁 —— 锁就这么残留下来了。

**正确姿势**：git 命令的输出一律用**文件重定向**，不要接 `head` / `tail`：

```bash
git push > /tmp/push.log 2>&1; echo "EXIT=$?"; cat /tmp/push.log
```

**排查顺序**（别一上来就无脑删锁）：

1. 确认无活跃 git 进程（`pgrep -fl git`，注意 Clash/微信会有误匹配）。
2. `rm -f .git/index.lock .git/refs/remotes/origin/*.lock`
3. 重跑命令，**不接管道**。
4. 若仍报锁，看完整报错里的具体路径 —— 很可能不是 `index.lock` 而是 `refs/...lock`（两者的报错文案一模一样，极易误判）。

> 附注：沙箱环境下 `rm` 可能报 `Operation not permitted`，需在非沙箱模式执行。

### 5.4 浏览器验证思路（用 agent-browser 或手动）

验证顺延链路：打开页面后，用控制台构造「昨天晚间读书/学习未完成」，再调 `planRefreshToday()`，检查：
- 规划页晚间出现「⏳ 延期」任务；
- 首页 banner / 读书卡 / 读书页 hero 显示「顺延」；
- 昨天已完成时，各页正常显示今日新章节。

### 5.5 数据迁移

- 页面右上角有「导出备份 / 导入备份」（JSON），换机一键恢复。
- 已有一次性迁移标记（`wb_life_agent_mig`、`wb_life_contact_mig` 等），加新迁移时沿用此模式，别直接改数据结构。

---

## 六、已知边界与待办

- **顺延起点只看「前一天」**：若某天根本没打开 App（没生成计划），欠账链会断。当前假设用户每天用，未做「往前追溯所有未完成」。如需更稳，可扩展 `planRolloverFrom` 向前多查几天。
- **规划模块设计稿 v0.4 的 P2/P3 未做**：周月视图的「完成后投影」、人脉维护占位转实装、AI 节奏建议（见 `设计文档-时间规划模块.md` 第六节）。
- **移动端**：设计时暂未同步规划模块，后续评估。
- `life-dashboard.html` 是旧版页面，已被 `mac-dashboard.html` 取代，保留未删。

---

## 七、本次会话优化清单（2026-08-26）

详情见 `CHANGELOG.md`，摘要：

1. **任务滚动顺延机制**（`917c131`）：未完成读书/学习自动延期，不再跳过。
2. **首页今日概览同步顺延 + 修复计划永久缓存**（`bd4df13`）：首页四处统一读每日计划；`PLAN_SCHEMA` + `planRefreshToday` 双兜底。
3. **休息日不再误生成学习任务**（`f5ac5ec`）：补 `type!=='rest'` 判断。
4. **读书页 hero 接入顺延 + 全站扫描收尾**（`b041d90`）：7 处展示入口全部收敛到 `getTodayDailyTasks()`。
