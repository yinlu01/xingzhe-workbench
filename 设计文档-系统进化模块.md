# 行者工作台 · 系统进化模块（Self-Evolving Loop）设计方案

> 版本：v0.2 ｜ 日期：2026-09-04 ｜ 状态：**MVP 已验证，待确认执行方案**
>
> 一句话：**让工作台每天根据自己的打卡数据和复盘反馈，向你提出改进建议；你点同意或不同意，系统就照做。**
> 目标是把「需求洞察 → 开发 → 验证」的迭代周期从「想起来才改」压缩到「每天一轮」。
>
> **v0.2 变更要点**：引入 SQLite 结构化存储层（`evolution_store.py`），与 localStorage **共存**。原 v0.1 的「快照导出」绕行方案降级为离线兜底，主通道改为**前端双写事件流**。同时修正一个长期认知错误：桌面 App 与手机 PWA 的数据**从未真正共享**（见 2.1）。

---

## 一、问题与定位

### 1.1 现状的三个断点

行者工作台已经有一个不错的反馈闭环（打卡 → 复盘 → 23:00 AI 分析 → 次日建议 → 昨日建议执行追踪）。但这条链路在今天断在最后一环：

| 环节 | 现状 | 断点 |
|---|---|---|
| 数据采集 | 打卡、复盘、AI 分析齐全 | ✅ 无断点 |
| 洞察生成 | AI 分析里有 `suggestion` 字段，质量不错 | ✅ 无断点 |
| **洞察 → 行动** | 建议只是**文字**，躺在复盘页等你自觉执行 | ❌ **断在这里** |
| **系统自我改进** | 功能改进靠你某天想起来跟 AI 提需求 | ❌ **断在这里** |

具体问题：

1. **建议不落地**：9/3 的 AI 分析里已经写得非常准——「阅读停在 26% 连续 6 天未动」「运动连续两天空缺」「简历无动作」。但这些都只是文字，第二天没有任何东西强制把它变成日程里的实际调整。
2. **产品迭代靠随机**：工作台的功能改进目前完全依赖「你某天想到了提一嘴」。9/2 的「今天按钮消失」就是典型——用了好几天才被发现，因为没有一个机制主动审视系统本身。
3. **没有偏好沉淀**：你拒绝过的建议、反复提的要求，没有被结构化记录下来，导致 AI 每次都从零开始理解你。

### 1.2 模块定位

**系统进化模块 = 在工作台上开一个「产品需求收件箱」，发件人是 AI，收件人是你，处理动作只有两个：同意 / 不同意。**

它不是：

- ❌ 又一个 AI 聊天框（不做对话，只做决策）
- ❌ 自动执行一切的失控代理（高风险改动必须你点头）
- ❌ 后台静默改代码的黑盒（每次改动都要写日志、可回滚）

它是：

- ✅ **决策界面**：每天 2–4 条具体建议，30 秒内处理完
- ✅ **执行通道**：你点头 → 后台真的改（改日程 / 改代码）
- ✅ **偏好记忆**：你的每次同意和拒绝都沉淀成画像，越用越懂你

### 1.3 与现有模块的关系

```
     ┌──────────── 现有闭环 ────────────┐
打卡 → 复盘 → 23:00 AI 分析 → 次日建议文字
                                   ↓
                          【系统进化模块】← 本设计
                    建议结构化 + 你的决策 + 后台执行
                                   ↓
              改日程（plan_patch） / 改代码（git commit）
                                   ↓
                    次日打卡数据验证改动是否有效
                                   ↓
                    再进入 AI 分析 → 下一轮建议
```

**关键增益**：改动的效果会被第二天的打卡数据自动检验，形成「假设 → 实验 → 验证」的真闭环，而不是改完就忘。

---

## 二、架构核心：跨 localStorage 的数据通道

### 2.1 必须解决的技术约束

这是本设计**最关键的一节**，先说清楚限制，否则方案无从谈起。

| 数据 | 存在哪 | 谁能写 | 谁能读 |
|---|---|---|---|
| 打卡记录、每日计划、待办 | 浏览器 `localStorage`（前缀 `wb_life_`，25 个键） | 只有前端 JS | 只有前端 JS |
| 复盘、AI 分析 | `review.json`（磁盘） | 前端 POST + 自动化 Agent | 前端 + Agent |
| **行为事件流** | **SQLite `workbench.db`**（磁盘） | **前端 POST + Agent** | **前端 + Agent** |
| 建议、反馈、补丁 | 新建的 JSON 文件（磁盘） | 前端 + Agent | 前端 + Agent |

**矛盾**：自动化 Agent 在 06:30 生成建议时，需要读你的打卡数据——但打卡数据在 localStorage 里，Agent **读不到**。反过来，Agent 生成的建议如果要改明天的日程，也**写不进** localStorage。

**额外发现（v0.2）**：侧边栏底部写着「数据与手机版共享」，`mac-dashboard.html:1821` 注释写着 `DATA KEYS (shared with mobile version)`——**但这个共享从未成立**。桌面 App 的 origin 是 `127.0.0.1:8765`，手机 PWA 是 `192.168.x.x:8765`，不同 origin 的 localStorage 物理隔离。25 个键在两边各存一份，**键名相同、数据无关**。目前真正跨设备共享的只有走服务端的 `review.json`。

> 这个发现改变了模块定位：进化模块不止要「让 Agent 能读数据」，还顺带成了**跨设备数据打通的契机**。

### 2.2 解决方案：双写事件流 + 补丁回灌

**主通道（v0.2 确立）**：前端在写 localStorage 的同时，**异步 POST 一份事件到服务端写入 SQLite**。localStorage 仍是主存储与唯一读取源，页面行为完全不变；SQLite 纯粹为分析服务。

```
【上行 · 双写】                          【下行 · 补丁】
  打卡动作                                  Agent 08:00 写补丁
     ├─→ localStorage（主，照常）            plan_patch.json
     └─→ POST /api/event ──→ SQLite             ↓
              （异步，失败静默）            前端轮询 → 自己应用到 localStorage
```

**为什么「双写」优于 v0.1 的「快照导出」**：

| 维度 | 快照导出（v0.1） | 双写事件流（v0.2） |
|---|---|---|
| 数据粒度 | 每日整包快照，只有当天状态 | 每条行为一条事件，带精确时间戳 |
| 历史补全 | 只能从某天开始积累 | 可回填（已从 review.json 回填 11 天） |
| 查询能力 | 前端算好 gap 给 Agent | Agent 直接 SQL 聚合，想问什么问什么 |
| 跨设备 | 仍是两套数据 | 天然汇聚到一处，顺带打通 |
| 失败影响 | 前端逻辑，失败影响快照 | 异步静默失败，不影响打卡 |

`checkin_snapshot.json` 保留为**离线兜底**：服务端不可达时前端暂存事件，恢复后批量补传。

### 2.3 为什么不做「全量迁移到数据库」

统计口径：`mac-dashboard.html` 中 localStorage 读写调用点共 **228 处**（`ld()` 64 / `sv()` 40 / `lg()` 74 / `sg()` 27 / 直接调用 23），分布在全部 7 个页面。

一次性重写 = 高风险大爆炸，且会破坏「单文件零构建、离线可用」的核心原则。

**结论：共存，不迁移。** localStorage 继续当主存储（快、离线、简单），SQLite 当分析库（可查询、可共享、Agent 可读）。两者职责不重叠，也不需要强一致——分析数据晚几秒甚至缺一两条，不影响任何结论。

---

```
【上行】localStorage → 磁盘                    【下行】磁盘 → localStorage
  前端每次打卡后 / 每日首次打开                    前端 60s 轮询
         ↓                                              ↑
  POST /api/checkin-snapshot                    GET suggestions.json
         ↓                                      GET plan_patch.json
  checkin_snapshot.json  ──→ Agent 读（06:30）         ↑
                                                       │
                                          Agent 写（08:00 / 21:00 批次）
```

**为什么不用现有方案替代？**

- ❌ 用 `review.json` 的 `checkSnapshot` 代替？不行——它只在**写复盘那一刻**才有，且只有当天的布尔快照，没有近 7 天趋势。
- ❌ Agent 直接改 localStorage？做不到，浏览器沙箱隔离。
- ❌ 让前端直接改代码？不该由前端承担业务逻辑，且无法做 git 版本管理。

**补丁回灌的设计精髓**：Agent 不直接改你的日程，而是写一个 `plan_patch.json`（声明式补丁），前端读到后自己应用到 localStorage。这样既绕开了限制，又保留了「前端是唯一写入方」的清晰边界，改动还可追溯（补丁文件就是审计日志）。

---

## 三、数据流全景

```
① 数据采集（前端，双写）
   打卡动作 ─┬─→ localStorage（主存储，页面照常读这里）
             └─→ POST /api/event（异步，失败静默）→ SQLite events 表
                        ⚡ 触发时机：每次打卡 / 完成任务 / 写复盘
                        🛟 离线兜底：服务端不可达时暂存 checkin_snapshot.json，恢复后补传

② 建议生成（Agent，每日 06:30）
   查 SQLite（打卡趋势，直接 SQL 聚合）
   + review.json 近 7 天（复盘原文 / energy / prevSuggestionDone / aiSummary）
   + evolve_feedback.json（你的历史决策）
   + evolve_profile.json（偏好画像）
        ↓ 分析 → 生成 2–4 条建议
   写 suggestions.json

③ 建议展示（前端，60s 轮询）
   GET suggestions.json → 渲染建议卡
   首页 mini 卡显示「今日 N 条待决策」+ 红点

④ 你的决策（前端，随时）
   点【同意】/【不同意】→ 展开补充说明框 → 提交
   POST /api/evolve-feedback → evolve_feedback.json

⑤ 执行（Agent，每日 08:00 / 21:00 两批次）
   读 evolve_feedback.json 中未处理的决策
     ├─ type=plan   → 生成补丁写 plan_patch.json → 前端应用 → 次日日程生效
     └─ type=system → 改 mac-dashboard.html → 先 git commit 建还原点 → 再改 → 写日志
   回写 suggestions.json 中该条 status=applied + 执行结果
   更新 evolve_profile.json（采纳率 / 拒绝模式）

⑥ 验证（自动）
   改动生效后的打卡数据进入下一轮 ①，AI 分析时会对比改动前后效果
```

---

## 四、建议的两大类型与风险分级

### 4.1 类型定义

| 类型 | `type` | 说明 | 执行方式 | 生效时间 |
|---|---|---|---|---|
| **规划改进** | `plan` | 调整你的日程：任务时段、任务数量、锚点时长、顺延策略、提醒时间 | Agent 写 `plan_patch.json`，前端应用 | 次日 / 即时 |
| **系统功能** | `system` | 改工作台本身：新增功能、修 bug、交互优化、新增自动化 | Agent 直接改 `mac-dashboard.html` / `server.py`，提交 git | 重启 App 后 |

### 4.2 风险分级（决定要不要你二次确认）

| 级别 | 含义 | 处理方式 | 举例 |
|---|---|---|---|
| **L0** | 纯数据微调，可完全回退 | 建议中标注「已自动应用」，你可在 24h 内撤销 | 调提醒时间、调任务显示顺序 |
| **L1** | 影响你的日程安排 | **需同意**，由补丁文件应用 | 把阅读从晚间挪到通勤、每日任务数上限 3 件 |
| **L2** | 改动代码，有回归风险 | **需同意 + 二次确认弹窗**，执行前自动 git commit | 新增一个统计卡片、修改规划页交互 |

> **硬规则**：`type=system` 的建议一律不低于 **L2**；**绝不**自动执行 L2。

### 4.3 建议质量的硬约束

这是本模块成败的关键——**烂建议会让你三步之内弃用整个模块**。每条建议必须同时满足：

1. **有数据依据**（`evidence`）：必须引用真实数据，如「阅读连续 4 天停在 26%」，不接受「建议你多读书」这种正确的废话。
2. **动作具体到可直接执行**（`action`）：写清楚改什么、改成什么。❌「优化你的时间分配」→ ✅「把 9/5 的阅读任务从晚间移到上午，锚点 15 分钟 / 5 页」。
3. **说清预期效果**（`expected`）：改完会怎样，用什么数据验证。
4. **成本可见**：这条建议需要你付出什么（时间 / 习惯改变），写明白。
5. **去重**：同一数据信号在 **7 天内不重复建议**；被拒绝过的同类建议进入冷静期。

**每日建议上限：4 条**（规划类最多 3 条 + 系统类最多 1 条）。宁可少而准。

---

## 五、界面设计

### 5.1 侧边栏新增入口

在「技能」下方新增一项，命名 **「进化」**，图标用循环箭头（`↻`），带待决策数量红点徽章：

```
侧边栏：
  🏠 首页
  📅 规划
  💪 运动
  📚 读书
  👥 人脉
  ✍️ 复盘
  🎯 技能
  ↻ 进化 ③      ← 新增，③ = 待决策建议数
```

### 5.2 进化页布局

```
┌────────────────────────────────────────────────────────────┐
│  系统进化                          今日 3 条待决策 · 采纳率 68%  │
│  ────────────────────────────────────────────────────────  │
│  子 Tab： 待决策 (3) │ 已处理 │ 改动记录                      │
├────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ 建议卡 ─────────────────────────────────────────────┐   │
│  │ 【规划改进 · L1】  依据：近 7 天数据 · 生成于 06:30      │   │
│  │                                                        │   │
│  │  把阅读从晚间挪到上午，锚点降到 15 分钟                  │   │
│  │                                                        │   │
│  │  📊 依据                                                │   │
│  │    阅读连续 4 天停在 26%（8/31 起未推进）；              │   │
│  │    晚间时段近 7 天完成率仅 40%，被运动和学习挤压；        │   │
│  │    你在 9/3 复盘里写：「运动之后精力不足影响看书」        │   │
│  │                                                        │   │
│  │  🎯 动作                                                │   │
│  │    9/5 的阅读任务从「晚间」移到「上午」；                 │   │
│  │    目标改为最小锚点 15 分钟 / 5 页；                     │   │
│  │    晚间任务数上限设为 2 件                               │   │
│  │                                                        │   │
│  │  ✅ 预期  打破连续静止，阅读百分位当天有变动               │   │
│  │  💰 成本  只需换个时段，总量不增加                        │   │
│  │                                                        │   │
│  │  [ 👍 同意 ]  [ 👎 不同意 ]                             │   │
│  │  ┌ 补充说明（可选）────────────────────────────┐       │   │
│  │  │ 上午也不行，改成通勤时段吧                    │       │   │
│  │  └────────────────────────────────────────────┘       │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─ 建议卡（系统功能 · L2）─────────────────────────────┐    │
│  │  运动打卡增加「一键补记昨天」                            │   │
│  │  📊 依据：近 14 天有 5 天运动数据为空，其中 3 天次日      │   │
│  │     复盘里提到「昨天其实练了但忘了打卡」                  │   │
│  │  ⚠️  影响：运动模块打卡区 + 复盘快照                     │   │
│  │  🔄 回滚：改动前已建 git 还原点                          │   │
│  │  [ 👍 同意并执行 ]  [ 👎 不同意 ]                       │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                              │
├────────────────────────────────────────────────────────────┤
│  已处理（近 7 天）                                            │
│   9/3   ✅ 已应用  阅读锚点降到 5 页            → 阅读推进 +2pt │
│   9/2   ❌ 已拒绝  增加晨间复盘                  → 理由：太麻烦  │
└────────────────────────────────────────────────────────────┘
```

### 5.3 首页 mini 卡

首页新增一张「今日建议」卡，位置在复盘状态卡下方：

```
┌─ 今日建议 ──────────────────┐
│  ↻ 3 条待你决策              │
│  最新：把阅读挪到上午…        │
│  [ 去处理 → ]                │
└──────────────────────────────┘
```

无建议时显示「今日无新建议 · 数据健康」，不占视觉干扰。

### 5.4 决策交互细节

- **同意**：按钮点击后立即变成「已同意 · 等待执行」，附执行批次时间（如「将在 08:00 批次执行」）。
- **不同意**：展开必填的理由标签（单选，可多选）+ 文本框：
  - `时机不对` / `不适合我` / `成本太高` / `已有类似安排` / `不认同这个判断` / `其他`
  - 选「其他」时文本框必填
- **补充说明（同意时也可填）**：同意不代表完全照做，你可以在框里改要求，如「同意但改成通勤时段」。Agent 执行时**以你的补充说明为准**。
- **撤销窗口**：L1 建议执行后 24 小时内，卡片上仍有「撤销」按钮，点击生成反向补丁。

---

## 六、数据结构定义

### 6.0 SQLite 表结构（`workbench.db`，主分析库）

事件溯源设计：**记「发生了什么」，不记「现在是什么状态」**。这样以后想问任何新指标都能重算。

```sql
-- 事实表：只追加，永不修改
CREATE TABLE events (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    ts         TEXT NOT NULL,      -- 事件发生时刻（ISO）
    date       TEXT NOT NULL,      -- 归属日期 YYYY-MM-DD
    type       TEXT NOT NULL,      -- checkin.reading / checkin.agent / exercise / task.done / review.saved / skill.practice
    payload    TEXT,               -- JSON 明细
    source     TEXT,               -- mac-app / pwa / agent / backfill（来源可追溯）
    created_at TEXT
);
CREATE INDEX idx_events_date ON events(date);
CREATE INDEX idx_events_type ON events(type, date);

-- 物化视图：按天聚合，供 06:30 建议生成秒查
CREATE TABLE daily_metrics (
    date             TEXT PRIMARY KEY,
    reading_done     INTEGER DEFAULT 0,
    reading_pct      REAL,
    reading_book     TEXT,
    agent_done       INTEGER DEFAULT 0,
    study_done       INTEGER DEFAULT 0,
    plan_done        INTEGER DEFAULT 0,
    exercise_done    INTEGER DEFAULT 0,
    exercise_minutes INTEGER DEFAULT 0,
    exercise_types   TEXT,
    skill_count      INTEGER DEFAULT 0,
    task_total       INTEGER,
    task_done        INTEGER,
    task_overdue     INTEGER,
    review_written   INTEGER DEFAULT 0,
    energy           INTEGER,
    prev_suggestion_done INTEGER,
    review_chars     INTEGER
);
```

存储层实现：`evolution_store.py`（Python 标准库 `sqlite3`，零外部依赖）。

| 命令 | 用途 |
|---|---|
| `python3 evolution_store.py init` | 建表（幂等） |
| `python3 evolution_store.py backfill` | 从 `review.json` 回填历史（幂等，重复执行不产生重复行） |
| `python3 evolution_store.py stats` | 查看数据概况 |
| `python3 evolution_store.py query "<SQL>"` | 自定义查询 |

### 6.1 `checkin_snapshot.json`（离线兜底，降级为可选）

```json
{
  "schema": 1,
  "exportedAt": "2026-09-04T22:15:03",
  "today": "2026-09-04",
  "streaks": {
    "reading": 0, "agent": 0, "exercise": 0,
    "review": 11, "skill": 0
  },
  "last7": {
    "reading": {
      "days": [
        {"date": "2026-09-03", "done": false, "pct": 26, "chapter": "第 6 讲"}
      ],
      "doneCount": 2, "pctStart": 26, "pctEnd": 26, "book": "战略与路径"
    },
    "agent":   {"days": [{"date": "2026-09-03", "done": true, "topic": "RAG"}], "doneCount": 1},
    "exercise":{"days": [{"date": "2026-09-03", "types": ["拉日"], "minutes": 0}], "doneCount": 2, "totalMinutes": 95},
    "plan": {
      "days": [
        {"date": "2026-09-03", "total": 5, "done": 2, "rate": 0.4,
         "bySeg": {"morning": {"total":1,"done":0}, "afternoon": {"total":2,"done":1}, "evening": {"total":2,"done":1}}}
      ],
      "avgRate": 0.43
    },
    "review": {"days": [{"date": "2026-09-03", "written": true, "energy": 4, "prevSuggestionDone": null}]},
    "skill":  {"days": [{"date": "2026-09-03", "count": 0}], "totalCount": 1}
  },
  "task": {"overdue": 3, "todayTotal": 4, "todayDone": 1},
  "gaps": [
    "reading 连续 4 天未推进（8/31 起停在 26%）",
    "exercise 连续 2 天空窗",
    "晚间时段完成率 40%，为三段中最低"
  ]
}
```

> `gaps` 字段由前端用固定规则算出（连续未打卡天数、完成率最低时段、逾期数），给 Agent 一个结构化的起点，减少误判。

### 6.2 `suggestions.json`（Agent 写 → 前端读）

```json
{
  "schema": 1,
  "date": "2026-09-05",
  "generatedAt": "2026-09-05T06:30:00",
  "basedOn": {
    "snapshotAt": "2026-09-04T22:15:03",
    "reviewDates": ["2026-09-03", "2026-09-04"],
    "hasAiSummary": true
  },
  "items": [
    {
      "id": "20260905-01",
      "type": "plan",
      "risk": "L1",
      "title": "把阅读从晚间挪到上午，锚点降到 15 分钟",
      "evidence": "阅读连续 4 天停在 26%；晚间时段近 7 天完成率 40%（三段最低）；9/3 复盘原话「运动之后精力不足影响看书」，判断为时段冲突而非意愿问题。",
      "action": "9/5 的 reading 任务从 evening 段移到 morning 段，目标改为最小锚点 15 分钟 / 5 页，晚间任务数上限设为 2。",
      "expected": "阅读百分位当天产生变动，打破连续 4 天静止；验证指标：9/5 checkSnapshot.reading.pct > 26。",
      "cost": "只需换个时段，总量不增加",
      "status": "pending",
      "decidedAt": null,
      "decisionNote": "",
      "executedAt": null,
      "result": null
    },
    {
      "id": "20260905-02",
      "type": "system",
      "risk": "L2",
      "title": "运动打卡增加「一键补记昨天」",
      "evidence": "近 14 天有 5 天运动数据为空，其中 3 天的次日复盘提到「昨天练了但忘了打卡」。",
      "action": "在运动模块打卡区增加「补记昨天」按钮，写入 wb_life_exercises 时日期取昨天，并同步刷新复盘快照。",
      "expected": "运动数据完整率从 64% 提升到 90%+，AI 分析的归因准确度提升。",
      "cost": "新增约 20 行代码，改动限制在运动模块内",
      "impact": "运动模块打卡区 + 复盘快照生成",
      "rollback": "改动前自动 git commit 建立还原点，可在改动记录页一键回滚",
      "status": "pending",
      "decidedAt": null,
      "decisionNote": "",
      "executedAt": null,
      "result": null
    }
  ]
}
```

**状态机**：

```
pending ──同意──→ approved ──执行成功──→ applied
   │                  │                     │
   │                  └──执行失败──→ failed  └──24h内──→ 可撤销 → reverted
   └──不同意──→ rejected（记录理由标签）
   └──超过 48h 未决策──→ expired（进「已处理」，标注未决策）
```

### 6.3 `evolve_feedback.json`（前端写 → Agent 读）

```json
{
  "schema": 1,
  "items": [
    {
      "suggestionId": "20260905-01",
      "date": "2026-09-05",
      "decision": "approved",
      "note": "同意但改成通勤时段，上午要开会",
      "reasonTags": [],
      "decidedAt": "2026-09-05T08:12:31",
      "processed": false,
      "processedAt": null,
      "batch": null
    },
    {
      "suggestionId": "20260905-02",
      "date": "2026-09-05",
      "decision": "rejected",
      "note": "",
      "reasonTags": ["成本太高", "时机不对"],
      "decidedAt": "2026-09-05T08:13:02",
      "processed": false,
      "processedAt": null,
      "batch": null
    }
  ]
}
```

> `note` 同意时也有效——Agent 执行时**以补充说明覆盖原始 action**。

### 6.4 `plan_patch.json`（Agent 写 → 前端应用）

声明式补丁，前端是唯一的 localStorage 写入方：

```json
{
  "schema": 1,
  "generatedAt": "2026-09-05T08:00:12",
  "items": [
    {
      "patchId": "patch-20260905-01",
      "suggestionId": "20260905-01",
      "targetDate": "2026-09-05",
      "note": "已按你的说明将目标时段改为通勤（morning 段）",
      "ops": [
        {"op": "move",   "taskType": "reading", "from": "evening", "to": "morning"},
        {"op": "update", "taskType": "reading", "fields": {"detail": "第 7 讲 · 通勤时段 15 分钟 / 5 页"}},
        {"op": "cap",    "seg": "evening", "maxTasks": 2}
      ],
      "applied": false,
      "appliedAt": null
    }
  ]
}
```

**支持的 op（P0 只实现前 4 个）**：

| op | 含义 | 参数 |
|---|---|---|
| `move` | 移动任务到另一时段 | `taskType`, `from`, `to` |
| `add` | 新增任务 | `seg`, `task` |
| `update` | 修改任务字段 | `taskType`, `fields` |
| `cap` | 限制某时段任务数上限 | `seg`, `maxTasks` |
| `remove` | 移除任务（P1） | `taskId` |
| `reschedule` | 调整提醒时间（P1） | `type`, `time` |

前端应用后 `POST /api/patch-applied` 回写 `applied: true`，页面提示「系统已按你的同意调整了明日规划」。

### 6.5 `evolve_profile.json`（Agent 维护，偏好画像）

```json
{
  "schema": 1,
  "updatedAt": "2026-09-05T08:00:12",
  "stats": {"total": 24, "approved": 16, "rejected": 8, "rate": 0.67},
  "typeStats": {
    "plan":   {"approved": 12, "rejected": 4, "rate": 0.75},
    "system": {"approved": 4,  "rejected": 4, "rate": 0.50}
  },
  "topRejectReasons": ["成本太高", "时机不对"],
  "muted": [
    {"tag": "increase-checkin-items", "reason": "连续 3 次拒绝「新增打卡项」类建议", "until": "2026-09-19"}
  ],
  "preferences": [
    "偏好小步改动，拒绝显著增加日常负担的建议",
    "对「新增打卡项」类建议敏感，尽量在现有模块内优化",
    "同意时经常附带调整要求，需预留补充说明空间"
  ]
}
```

**冷却规则**：同一 `tag` 的建议连续被拒 **3 次** → 该类建议静默 **14 天**，`muted` 数组记录到期时间。

---

## 七、后端接口（server.py 新增）

沿用现有 `/api/review` 的实现模式（读 body → 读写 JSON → 返回）。

| 方法 | 路径 | 用途 | 写入目标 |
|---|---|---|---|
| **POST** | **`/api/event`** | **双写通道：前端行为事件落库（追加）** | **SQLite `events`** |
| POST | `/api/checkin-snapshot` | 离线兜底：服务端不可达时的暂存快照，恢复后补传 | `checkin_snapshot.json` |
| POST | `/api/evolve-feedback` | 提交决策，按 `suggestionId` 去重合并更新 | `evolve_feedback.json` |
| POST | `/api/patch-applied` | 标记补丁已被前端应用 | `plan_patch.json` |
| GET | `/suggestions.json`、`/plan_patch.json` | 静态文件，`SimpleHTTPRequestHandler` 已自动支持，无需额外代码 | — |

> `/api/event` 必须**异步且失败静默**——服务端没启动时打卡照常可用，绝不因分析库故障影响主功能。

> 注意：这些文件与 `review.json` 一样**加入 .gitignore**（个人数据，不入库）。

---

## 八、前端改动点

| # | 位置 | 改动 |
|---|---|---|
| 1 | 侧边栏（约 1163 行后） | 新增 `data-tab="evolve"` 的 nav-item + 待决策数徽章 |
| 2 | 页面容器 | 新增 `<div class="page" id="page-evolve">` |
| 3 | `switchTab` / `renderAll` | 接入新 tab 的渲染分支 |
| 4 | 新增函数组 | `renderEvolve()` / `renderSuggestionCard()` / `submitDecision()` / `exportCheckinSnapshot()` / `applyPlanPatches()` |
| 5 | `autoRefreshData()` | 增加轮询 `suggestions.json`（新建议上屏 + 系统通知）与 `plan_patch.json`（有新补丁则应用） |
| 6 | 打卡函数 | `checkPlanToday` / `togglePlanCheckIn` 等打卡入口后调用 `exportCheckinSnapshot()`（防抖 3 秒） |
| 7 | 首页 | 新增「今日建议」mini 卡 |
| 8 | 导出/导入备份 | 备份文件纳入 `wb_life_evolve_decisions`（本地决策缓存，离线也能看历史） |

**关键实现约束**：

- 前端轮询到 `suggestions.json` 变化后，与本地 `wb_life_evolve_seen` 比对，只对新建议弹通知。
- 断网 / 文件不存在时静默失败，绝不报错打断使用（沿用现有 `autoRefreshData` 的 try-catch 风格）。
- 补丁应用必须幂等：用 `applied` 标记 + 本地已应用 `patchId` 集合去重，防止重复应用。

---

## 九、自动化任务（新增 3 个）

| 时间 | 任务名 | 动作 |
|---|---|---|
| **06:30** | 生成每日改进建议 | 读 `checkin_snapshot.json` + `review.json` + `evolve_feedback.json` + `evolve_profile.json` → 生成 2–4 条建议 → 写 `suggestions.json` → 更新 `basedOn` |
| **08:00** | 处理进化反馈（晨间批次） | 读未处理决策 → `plan` 类写 `plan_patch.json`；`system` 类改代码 + git commit → 回写状态与 `evolve_profile.json` |
| **21:00** | 处理进化反馈（晚间批次） | 同上；额外清理超 48h 未决策的 `pending` → 标记 `expired` |

**与现有自动化的时间关系**：

```
00:05 读书计划推进
06:30 【新】生成改进建议          ← 读昨晚 23:00 的 AI 分析
07:00 每日自测
08:00 【新】处理反馈（晨批）       ← 你早上打开 App 后的决策
08:30 微信读书同步
...
21:00 【新】处理反馈（晚批）
22:30 读书笔记总结 / 复盘提醒
23:00 复盘 AI 分析               ← 第二天 06:30 的建议会用到
23:50 前端兜底导出快照
```

---

## 十、验收标准

| # | 验收项 | 标准 |
|---|---|---|
| 1 | 建议按时生成 | 连续 7 天，每天 06:30 后打开页面能看到 2–4 条新建议，`evidence` 均引用真实数据 |
| 2 | 决策实时落地 | 点击同意后 5 秒内 `evolve_feedback.json` 出现该条记录，页面状态变「已同意 · 等待执行」 |
| 3 | 规划建议可执行 | 同意一条 `plan` 建议后，次日规划页能看到调整生效，且卡片显示「已应用」+ 调整摘要 |
| 4 | 系统建议可追溯 | 同意一条 `system` 建议后，`mac-dashboard.html` 产生改动，git 有独立提交，改动记录页可见 |
| 5 | 拒绝有学习效应 | 同一类建议连续拒绝 3 次后，14 天内不再出现同类建议 |
| 6 | 补充说明优先生效 | 同意时填写「改成通勤时段」，执行结果按补充说明调整而非原始建议 |
| 7 | 零干扰保障 | 数据文件缺失 / 网络异常时，页面无任何报错，其他模块功能不受影响 |

---

## 十一、分期实施计划

| 阶段 | 范围 | 交付物 | 预估改动 | 状态 |
|---|---|---|---|---|
| **MVP-0** | 存储层验证 | SQLite 建库 + 历史回填 + 分析查询 | `evolution_store.py`（已完成） | ✅ **已完成并验证** |
| **P0** | 跑通数据闭环 | 前端双写 → 建议生成 → 展示 → 决策采集。`plan` 类建议**只生成补丁文件，由 Agent 在下次对话时手动应用**（先不自动） | server.py +1 接口；mac-dashboard.html 新增进化页约 400 行 + 双写钩子约 30 行；+2 自动化任务 | 待确认 |
| **P1** | 自动执行与学习 | `plan_patch` 自动应用 + `evolve_profile` 偏好学习 + 采纳率统计 + 撤销窗口 | 前端补丁应用器约 150 行；+1 自动化任务 | 待排期 |
| **P2** | 系统自我改动 | `system` 类建议自动编码 + 改动记录时间线 + git 还原点回滚 + 效果验证看板 | 改动记录页约 200 行；Agent 侧执行脚本 | 待排期 |

**MVP-0 先行的理由**：在投入 400 行前端代码之前，先用一个零侵入的脚本验证最根本的假设——**结构化数据能不能问出有价值的问题**。如果能，再建通道；如果不能，整个模块的设计前提就不成立。验证结果见第十五节。

---

## 十二、风险与边界

| 风险 | 影响 | 应对 |
|---|---|---|
| **建议质量差 → 你弃用** | 模块形同虚设 | 每日上限 4 条；`evidence` 强制引用数据；7 天去重；前两周建议以「观察性」为主，少提改动 |
| **自动改代码引入 bug** | 工作台故障 | L2 必须二次确认；执行前强制 git commit 还原点；单次改动限制在单模块内；改动后自测脚本验证 |
| **建议疲劳** | 每天一堆待决策变成新负担 | 上限 4 条；48h 未决策自动过期不堆积；红点只显示待决策数 |
| **自动化任务失败** | 建议断供 | 页面显示建议生成时间，超 12 小时标黄提示；支持手动补跑（沿用现有复盘补跑预案） |
| **打卡快照不准** | 建议基于错误数据 | 快照带 `exportedAt` 时间戳，Agent 发现数据过旧（>24h）时在建议里注明数据可能滞后 |
| **补丁与用户手动改动冲突** | 日程被覆盖 | 补丁只操作 `op` 指定的任务，不动用户自定义任务；应用前检测目标日期计划是否已被手动修改 |

### 明确不做

- ❌ **AI 直接写 localStorage**（沙箱隔离做不到，且违反单一写入方原则）
- ❌ **自动执行 L2 系统改动**（必须有你的二次确认）
- ❌ **对话式交互**（这是决策界面，不是聊天框；要聊天请用主对话）
- ❌ **多人 / 云端建议库**（本地优先，N=1）
- ❌ **建议市场 / 模板分享**（无此诉求）

---

## 十三、待你确认的关键决策点

开始编码前，这几件事需要你拍板：

| # | 决策点 | 我的建议 | 备选 |
|---|---|---|---|
| 1 | **模块命名** | 「进化」（呼应自我迭代，图标 ↻） | 「改进」「建议箱」「共创」 |
| 2 | **生成时间** | 06:30（早于 07:00 自测，起床就能看到） | 23:30（复盘分析后立刻生成，但容易淹没在睡前信息里） |
| 3 | **每日建议条数** | 2–4 条（规划 ≤3 + 系统 ≤1） | 固定 3 条 |
| 4. | **处理批次** | 08:00 + 21:00 两批次 | 仅 08:00 一批（延迟高，早上同意可能要等到第二天） |
| 5 | **P0 是否自动应用规划补丁** | **否**——P0 只生成补丁，由对话里手动确认后应用 | 是——全自动（闭环更快，但出错风险高） |
| 6 | **系统类建议的改动权限** | 允许改 `mac-dashboard.html` 的非核心区（UI / 展示），**禁止**自动改规划引擎（`ensurePlanForDate` 等） | 完全开放 |

---

## 十四、修订记录

| 版本 | 日期 | 摘要 |
|---|---|---|
| v0.1 | 2026-09-04 | 首版：确立跨 localStorage 数据通道方案、双类型建议模型、三级风险分级、P0/P1/P2 分期 |
| v0.2 | 2026-09-04 | 引入 SQLite 存储层与 localStorage 共存；主通道改为双写事件流；修正「数据共享」认知错误；完成 MVP-0 验证并给出最终执行方案 |

---

## 十五、MVP-0 验证结果（2026-09-04 已跑通）

### 15.1 做了什么

**零侵入**：只新增 `evolution_store.py` 一个文件，`mac-dashboard.html` 与 `server.py` **一行未动**（`git diff` 已验证）。

从 `review.json` 回填了 2026-08-24 → 09-03 共 11 天历史数据（来源：每日 `checkSnapshot` 打卡快照 + energy + prevSuggestionDone），跑通 5 条分析查询，幂等性已验证（重复 backfill 产生 0 条重复行）。

### 15.2 数据问出了什么

**查询 1 — 阅读进度每日变化**

```
08-24  18%   ┐
08-25  20%  +2   8/24–8/28：稳定推进期（+8pt）
08-26  20%   0
08-27  23%  +3
08-28  26%  +3   ┘
08-29  26%   0   ┐
08-30  26%   0   │
08-31  26%   0   │ 8/29–9/3：完全停滞（最长停滞段 5 天）
09-01  ——   (无) │
09-02  26%   0   │
09-03  26%   0   ┘
```

**查询 3 — 11 天各维度完成率**

| 样本天数 | 阅读打卡率 | agent 学习率 | 运动率 | **复盘率** | 平均能量 |
|---|---|---|---|---|---|
| 11 | 18% | 9% | 18% | **91%** | 3.3 |

**查询 5 — 能量 vs 当日行为（反直觉）**

| 能量 | 天数 | 运动率 | 学习率 | 平均复盘字数 |
|---|---|---|---|---|
| 3 分 | 7 | 29% | 0% | 158 字 |
| 4 分 | 3 | **0%** | **33%** | **303 字** |

能量 4 分的日子反而**完全没运动**，但学习率和复盘的深入程度都是低能量日的约 2 倍。

### 15.3 最重要的发现：**问题的定性被数据推翻了**

| | 看复盘文字得出的印象 | 数据实际说的 |
|---|---|---|
| 问题是什么 | 「阅读停滞、运动空缺、排程太满」——**执行层**的毛病 | 复盘率 **91%**，但行动率全线 **<20%** |
| 缺的是什么 | 缺更好的计划 | 不缺计划，缺**从"知道"到"做"的转化** |
| 系统该做什么 | 给出更好的建议 | 建议已经够好了（23:00 AI 分析质量很高），**缺的是把建议变成日程里的实际动作并追踪** |

**这意味着进化模块的设计重心要调整**：

原设计偏重「生成高质量建议」。但数据显示，你复盘的自觉性（91%）远超执行率（<20%）——**再给一遍建议，边际收益很低**。真正的杠杆在于：

1. **把建议变成日程里的实际动作**（`plan_patch` 机制的价值被放大 → 建议从 P1 提到 P0）
2. **追踪执行并反馈**（`prevSuggestionDone` 目前 10 天里 10 次全是 `None`/未填 → 这个字段形同虚设，必须做成强制勾选）
3. **降低行动门槛**（最小锚点：不是"读一章"，而是"读 5 页"）

### 15.4 次要发现：样本不足时 SQL 会诚实报错

「运动是否影响次日阅读」这个假设——你 9/3 复盘里明确提到过——数据给的回答是：

| 场景 | 样本天数 | 阅读推进天数 | 推进率 |
|---|---|---|---|
| 运动日之后 | **1** | 0 | 0% |
| 未运动日之后 | 8 | 3 | 38% |

n=1，**不足以判断**。这正是结构化查询相对"凭感觉推断"的价值：它会暴露证据强度，而不是顺着你的叙事给结论。随着双写数据积累，两周后这个查询就有统计意义了。

---

## 十六、最终执行方案

### 16.1 原则

| 原则 | 说明 |
|---|---|
| **共存不迁移** | localStorage 继续当主存储，SQLite 只做分析。不做 228 处调用点的大重写 |
| **增量且可停** | 每个阶段独立可用，任何阶段停下都不影响现有功能 |
| **失败静默** | 分析库故障绝不影响打卡主流程 |
| **先验证再扩大** | 每个阶段跑够 7 天再进下一阶段 |

### 16.2 执行计划

| 阶段 | 内容 | 改动范围 | 风险 | 验收 |
|---|---|---|---|---|
| **A. 存储层** | SQLite 建库 + 历史回填 + 查询工具 | `evolution_store.py`（新增） | 无（只读现有文件） | ✅ 已完成：11 天数据入库，5 条查询跑通 |
| **B. 双写通道** | 前端打卡时异步 POST `/api/event`；server 端落库 | `server.py` +1 接口（约 25 行）；`mac-dashboard.html` 打卡钩子（约 30 行） | 极低：异步 + 失败静默，服务端挂了打卡照常 | 连续 7 天无遗漏；`mac-dashboard.html` 现有功能回归通过 |
| **C. 每天观察** | 每天用 `evolution_store.py query` 跑固定分析，验证数据质量 | 无代码改动（纯使用） | 无 | 能稳定回答「今日该关注什么」 |
| **D. 建议生成** | 06:30 自动化：查 SQLite → 生成 2–4 条建议 → 写 `suggestions.json` | 新增自动化任务 + 生成脚本（约 150 行） | 低：只写文件，不改前端 | 连续 7 天，每天 06:30 后能看到有数据依据的建议 |
| **E. 决策界面** | 「进化」页：建议卡 + 同意/不同意 + 补充说明 | `mac-dashboard.html` 新增页（约 400 行） | 中：新增页面，但不动现有页面逻辑 | 决策 5 秒内落盘；现有 7 个页面功能无回归 |
| **F. 自动执行** | `plan_patch` 自动应用 + 偏好学习 | 前端补丁应用器（约 150 行） | 中高：会真实改日程 | 同意后次日日程生效；24h 内可撤销 |

**建议节奏**：A（已完成）→ B → 观察 7 天 → D → 观察 7 天 → E → F。

B 和 D 之间留 7 天观察期是刻意的——先确认双写数据准确、查询能出稳定结论，再投入界面开发。

### 16.3 待确认事项

| # | 事项 | 建议 |
|---|---|---|
| 1 | 是否先只做 B（双写），观察 7 天再决定要不要继续 | **是**，成本极低而信息量极大 |
| 2 | 设计重心调整（从「生成建议」转向「把建议变成动作」）是否认可 | **建议认可**，这是数据给出的结论 |
| 3 | `prevSuggestionDone`（昨日建议执行勾选）目前 10 次全空，是否做成写复盘时的必填项 | **建议是**，否则执行追踪无从谈起 |
| 4 | 存储层文件命名 `evolution_store.py` / `workbench.db` 是否合适 | 可改 |
| 5 | 是否同步修掉「数据与手机版共享」这行误导性文案 | **建议修**，或真的把它实现（B 阶段后自然成立） |

---

## 十七、附录：常用分析查询

```sql
-- 阅读停滞区间（连续 0 增长）
WITH d AS (SELECT date, reading_pct,
                  ROUND(reading_pct - LAG(reading_pct) OVER (ORDER BY date),1) AS delta
           FROM daily_metrics WHERE reading_pct IS NOT NULL),
     g AS (SELECT date, reading_pct, delta,
                  SUM(CASE WHEN delta>0 THEN 1 ELSE 0 END) OVER (ORDER BY date) AS grp FROM d)
SELECT MIN(date) AS 起始, MAX(date) AS 结束, COUNT(*) AS 停滞天数, MAX(reading_pct) AS 卡在
FROM g WHERE delta=0 GROUP BY grp ORDER BY 停滞天数 DESC LIMIT 3;

-- 各维度完成率
SELECT COUNT(*) AS 天数,
       ROUND(100.0*SUM(reading_done)/COUNT(*),0) AS 阅读率,
       ROUND(100.0*SUM(agent_done)/COUNT(*),0)   AS 学习率,
       ROUND(100.0*SUM(exercise_done)/COUNT(*),0) AS 运动率,
       ROUND(100.0*SUM(review_written)/COUNT(*),0) AS 复盘率,
       ROUND(AVG(energy),2) AS 平均能量
FROM daily_metrics;

-- 运动日 vs 未运动日，次日阅读推进率（样本足够时才有意义）
SELECT CASE WHEN p.exercise_done=1 THEN '运动日之后' ELSE '未运动日之后' END AS 场景,
       COUNT(*) AS 样本天数,
       ROUND(100.0*SUM(CASE WHEN c.reading_pct > p.reading_pct THEN 1 ELSE 0 END)/COUNT(*),0) AS 推进率
FROM daily_metrics c JOIN daily_metrics p ON p.date = date(c.date,'-1 day')
WHERE c.reading_pct IS NOT NULL GROUP BY 场景;
```
