# 行者工作台 · 产品需求文档（PRD）

> 版本：v1.0 ｜ 创建：2026-08-29 ｜ 状态：正文定稿，随迭代持续更新
>
> 本文档是产品演进的**主文档**：记录「为什么做、为谁做、做到什么边界」，后续迭代以本文档为准绳。
> 配套文档：`README.md`（功能与技术架构）、`项目交接文档.md`（维护机制与坑）、`CHANGELOG.md`（改动历史）。

| 修订记录 | 日期 | 摘要 | 作者 |
|---|---|---|---|
| v1.0 | 2026-08-29 | 首版：覆盖六大模块 + 规划模块 + 自动化体系 | yinlu01 / 果果 |
| v1.1 | 2026-09-04 | 新增 4.10 系统进化模块（AI 建议 → 用户决策 → 后台执行的自我迭代闭环），设计稿见 `设计文档-系统进化模块.md` | yinlu01 / 果果 |

---

## 一、产品概述

### 1.1 一句话定位

**行者工作台是一个本地优先的个人成长操作系统**——把读书、学习、运动、人脉、待办、复盘六大日常收进一个界面，用「进度驱动的计划引擎」保证计划不因中断而作废，用 AI 复盘闭环让每天的执行被看见、被分析、被改进。

### 1.2 背景与问题

个人成长类需求长期被拆散在多个工具里，存在四个结构性问题：

1. **计划会被中断摧毁**：日历式计划按日期推进，一天没完成，第二天就跳到新内容，旧账永远丢了。市面工具（滴答清单、Notion 日历等）的「顺延」最多把任务挪一天，不会让**整条后续计划整体后移**。
2. **数据孤岛**：读书数据在微信读书，笔记在 Obsidian，打卡在各 App，复盘在备忘录——没有一个地方能回答「我今天该做什么、做到哪了」。
3. **执行无反馈闭环**：打卡工具只记录，不分析。连续状态、卡点识别、建议跟进，全靠人脑。
4. **云端依赖与隐私顾虑**：主流工具数据在云端，个人成长数据（精力状态、人脉记录、读书笔记）是高敏感数据，希望完全留在本机。

### 1.3 目标用户

**核心用户：产品构建者本人（N=1，产品经理 / 终身学习者）。**

画像要点：

- 每天有固定的成长例行：读书计划（创业者读书路线）、Agent 面试冲刺学习（12 周）、运动（一周六练）、人脉维护；
- 深度使用 Obsidian 做知识管理，希望工作台与既有知识库**互通**而非替代；
- 对数据主权敏感：接受不了成长数据放云上；
- 有技术协作能力：通过与 AI 结对完成产品迭代（本产品即「AI 协作开发的实践样本」）。

> N=1 是刻意选择而非局限：它使「需求洞察→开发→验证」链路压缩到一个人身上，是快速验证产品假设的最小闭环。若未来开放给同类用户，本文档即对外叙事的底稿。

### 1.4 产品目标与衡量指标

| 层级 | 目标 | 指标 |
|---|---|---|
| 北极星 | 「今日该做什么」永远准确且唯一 | 每日计划完整率（读书/学习/运动/复盘四类任务齐全），由每日 7:00 自测监控 |
| 习惯留存 | 用户持续使用且不弃用 | 复盘连续天数 🔥、学习打卡热力图连续性 |
| 计划有效性 | 中断后计划仍能恢复推进 | 顺延触发后次日计划正确展示「顺延章节」的比例（目标 100%，由一致性自愈机制保障） |
| 反馈闭环 | 每天的执行被 AI 分析并形成次日建议 | 昨日复盘 AI 分析生成率（目标 100%，由 23:00 自动化保障） |

### 1.5 竞品对照与差异化

| 维度 | 滴答清单/Things | Notion 模板 | 习惯打卡 App（Streaks 等） | **行者工作台** |
|---|---|---|---|---|
| 计划推进模型 | 日历驱动，顺延=挪一天 | 手动维护 | 仅打卡无计划 | **进度驱动（游标机制），中断即暂停、完成才推进，后续计划整体后移** |
| 数据位置 | 云端 | 云端 | 云端 | **全本机（localStorage + 本地 JSON）** |
| 数据集成 | 各自为政 | 手动录入 | 无 | **微信读书 API 自动同步 + Obsidian 双向归档** |
| 反馈闭环 | 无 | 无 | 无 | **每日 AI 复盘分析 + 昨日建议执行追踪** |
| 自动化 | 有限 | 无 | 无 | **每日 7 项定时自动化（同步/总结/分析/自测/提醒）** |
| 代价 | 订阅费 | 搭建成本 | 无 | 需自行维护（本地服务 + 桌面壳） |

**核心差异化**：不是「又一个待办工具」，而是**带计划引擎和反馈闭环的个人成长系统**。

---

## 二、核心设计理念（需求取舍的原则）

以下理念是所有功能需求的判断基准，后续新增功能若与下列原则冲突，默认不做：

1. **进度驱动，而非日历驱动**：计划跟着实际完成度走。没读完第三章，第四章就不该出现。这是本产品最重要的产品决策（v2.0 重构确立，commit `8a178ab`）。
2. **单一数据源**：全站 7 处「今日该做什么」的展示，统一由 `getTodayDailyTasks()` 一个出口驱动，任何割裂的数据口径都视为 bug。
3. **本地优先**：数据不出本机；唯一外部依赖是微信读书同步（用户自己的 API Key）。离线可用（PWA + Service Worker）。
4. **单文件、零构建、零依赖**：前端是单个 HTML 文件，无框架、无 CDN、无构建步骤——保证十年后打开还能跑，维护成本最低。
5. **自动化优先于手动**：凡是机器能做的（数据同步、笔记总结、复盘分析、健康自测）都交给定时任务，用户只做「打卡」和「写复盘」两个动作。
6. **自愈优于报错**：系统能检测数据不一致（计划章节 vs 游标）就自动重算，而不是把错误暴露给用户（`ensurePlanForDate` 一致性自愈）。

---

## 三、用户故事与核心场景

### 场景 A：中断后的恢复（产品灵魂场景）

> 我周二周三忙没读书，周四打开工作台——首页明确告诉我「今晚读第 3 讲（自周二顺延）」，而不是让我直接面对跳到的第 5 讲。学习计划同理整体后移。我补完第 3 讲打卡后，明天才出现第 4 讲。

**验收要点**：连续中断 N 天后打开 App，所有页面（首页/读书页/规划页/任务页）展示的章节一致且等于「最后一次完成章节的下一讲」；打卡后次日推进。

### 场景 B：一天的开始

> 早上 7:00 自测确认计划健康；9:00 收到工作台提醒。打开首页：今日待办、今晚读什么、学什么、练什么（今天周三=拉日）、该联系谁，一屏看完。

### 场景 C：一天的结束

> 21:30 若还没写复盘会收到提醒。写复盘时，系统弹出昨日 AI 建议让我勾选「已执行/未执行」，并自动带上今日打卡快照。23:00 AI 分析近 7 天复盘生成总览/亮点/卡点/明日建议，次日同步归档到 Obsidian。

### 场景 D：读书数据的自动流转

> 我在微信读书上划线、写想法，8:30 / 21:30 自动同步到工作台；22:30 AI 把近 3 天笔记总结成要点；首页「最近读书笔记」和「AI 读书笔记总结」直接可读，不需要任何手动录入。

### 场景 E：多设备同一数据

> 在 Mac 桌面 App 打卡，晚上躺床上用手机 PWA 看到的是同一份今日状态；复盘归档进 Obsidian 后，知识库自动出现在读书页的报告里。

---

## 四、功能需求（按模块）

优先级定义：P0 = 产品成立的基础，损坏即 P0 故障；P1 = 核心体验；P2 = 增强项。

### 4.1 首页 · 今日聚合（P0）

| 需求 | 说明 | 优先级 |
|---|---|---|
| 今日概览 banner | 今日待办数、顺延提示、读书/学习任务（统一口径） | P0 |
| 今日运动卡 | 按周训练计划显示今日训练内容 | P0 |
| 读书任务卡 | 今晚该读的章节（含顺延标记）、打卡入口 | P0 |
| Agent 学习卡 | 今日学习主题与交付物、进度条 | P0 |
| 复盘状态 | 连续天数 🔥、今日是否已写 | P1 |
| 人脉提醒 | 逾期/临期联系人高亮 | P1 |
| 最近读书笔记 + AI 总结 | 微信读书划线/想法 + 每日 AI 总结 | P1 |
| 60s 自动刷新 | review/weread/reading_plan 数据轮询，AI 分析新生成时上屏 + 系统通知 | P1 |

### 4.2 待办模块（P0）

- 核心目标 + 每日待办，按「逾期 / 今日 / 未完成 / 已完成」分组，逾期与今日截止自动高亮并推送通知。
- 待办是日历型数据，**不参与顺延引擎**（与读书/学习的进度型任务区分）。

### 4.3 运动模块（P0）

- 周训练计划固定（推日/搏击操/拉日/核心/腿日/有氧吊杠/休息），每日打卡（类型/时长/吊杠秒数），计入复盘快照。
- 运动是每日例行，**不参与顺延**（错过的训练不补，符合训练恢复逻辑）。

### 4.4 读书模块（P0）

| 需求 | 说明 | 优先级 |
|---|---|---|
| 进度驱动章节推进 | `reading_cursor` 游标指向序列中下一讲；打卡完成 → 游标 +1 → 次日生成新章节 | P0 |
| 微信读书自动同步 | 书架/在读/笔记/时长，每日 2 次 + 数据过期 30 分钟自动触发；**空响应防御**（API 返回空不覆盖本地数据） | P0 |
| 在读书目 | 优先展示微信读书在读（含进度% + 最近阅读日期），无数据 fallback 手动书目 | P1 |
| 读书笔记 | 优先展示微信读书近期划线/想法，与首页口径一致 | P1 |
| AI 读书笔记总结 | 每日 22:30 自动生成近 3 天笔记总结 | P1 |
| 打卡热力图 | 学习打卡历史可视化 | P2 |
| 顺延展示 | hero 区显示「第 X 讲（未完成 · 自 X/X 顺延）」 | P0 |

### 4.5 人脉模块（P1）

- 联系人 + 联系频率（如每 30 天），逾期/临期自动进首页提醒卡片并可推送通知；联系/拜访记录留痕。

### 4.6 复盘模块（P0）

| 需求 | 说明 | 优先级 |
|---|---|---|
| 每日复盘 | 文字 + 精力记录 + 当日打卡快照（学习/运动/读书/建议执行） | P0 |
| AI 每日分析 | 23:00 分析近 7 天，产出 summary/highlights/problem/suggestion 四字段 | P0 |
| 昨日建议跟进 | 写复盘时勾选昨日建议已执行/未执行，形成「建议→执行→检验」闭环 | P1 |
| Obsidian 归档 | 每日复盘 → `每日复盘/YYYY/MM/`，周报 → `周报/`，含打卡快照与 AI 分析 | P1 |
| 连续天数 | 自动统计并展示 | P1 |
| AI 周分析/周报 | 每周汇总 | P2 |

### 4.7 规划模块 · 计划引擎（P0，产品核心）

这是本产品区别于所有竞品的引擎层：

| 需求 | 说明 | 优先级 |
|---|---|---|
| 每日计划生成 | `ensurePlanForDate` 每晚生成 morning/afternoon/evening 三段计划 | P0 |
| 进度游标 | `reading_cursor` / `agent_cursor` 双游标；打卡驱动推进 | P0 |
| 任务顺延 | 昨日未完成的 reading/study 顺延至今日（`rolled` 标记 + 来源日期），顺延优先于新任务 | P0 |
| 单一数据源 | `wb_life_plan_daily` 是唯一事实源，`getTodayDailyTasks()` 是唯一出口，7 处展示入口全接入 | P0 |
| 一致性自愈 | 计划章节与游标不一致时自动失效重算 | P0 |
| 缓存双兜底 | `PLAN_SCHEMA` 版本号（逻辑变更 +1）+ `planRefreshToday()`（数据就绪重算） | P0 |
| 休息日处理 | Agent 计划中的周六 rest 日不生成新学习任务，但欠账仍顺延 | P0 |
| 打卡双向同步 | 打卡记录与计划任务 done 状态双向一致 | P0 |
| 周/月视图 | 规划页周计划展示 | P1 |

**明确不做**：自动打卡（曾上线后因与进度驱动模型冲突而下线，commit `887aa39`）；多日前欠账回溯（当前只看前一天，见 6.1）。

### 4.8 通知体系（P1）

- 双通道：macOS 原生通知（WKWebView 桥接）+ Web Push（RFC 8291，浏览器/手机）。
- 通知内容：逾期/截止待办、逾期联系人、今日训练、读书打卡、21:30 未写复盘提醒、AI 分析生成。

### 4.9 自动化体系（P1）

| 时间 | 任务 | 保障的指标 |
|---|---|---|
| 00:05 | 读书计划每日推进 | 计划按时生成 |
| **06:30** | **生成每日改进建议**（读打卡快照 + 复盘 → 写 suggestions.json） | 进化闭环启动 |
| 07:00 | **每日自测**（selfcheck.py：游标初始化 / 今日计划完整性 / 昨日复盘与 AI 分析） | 北极星指标监控 |
| **08:00** | **处理进化反馈（晨批）**：执行已同意的建议 | 决策 → 行动 |
| 08:30 / 21:30 | 微信读书同步 | 数据新鲜度 |
| 21:00 | 读书提醒 | 习惯留存 |
| **21:00** | **处理进化反馈（晚批）** + 过期建议清理 | 决策 → 行动 |
| 22:30 | 读书笔记 AI 总结 + 未写复盘提醒 | 反馈闭环 |
| 23:00 | 复盘 AI 分析 | 反馈闭环 |

> 加粗的三项属系统进化模块（4.10），2026-09-04 新增。

### 4.10 系统进化模块 · 自我迭代闭环（P1，2026-09-04 新增）

**定义**：工作台每天基于自己的打卡数据与复盘反馈，向用户提出改进建议；用户点同意或不同意（可附补充说明），后台据此真实执行——改日程或改代码。

**要解决的问题**：现有闭环断在最后一环——23:00 的 AI 分析产出了高质量 `suggestion`，但它只是文字，没有任何机制把它变成实际调整；产品功能改进则完全依赖用户随机提起。

| 需求 | 说明 | 优先级 |
|---|---|---|
| 建议生成 | 每日 06:30，基于打卡快照 + 近 7 天复盘 + 历史反馈 + 偏好画像，生成 2–4 条建议 | P1 |
| 双类型建议 | `plan`（调整日程）/ `system`（改工作台功能） | P1 |
| 三级风险 | L0 自动应用 / L1 需同意（改日程）/ L2 需同意 + 二次确认（改代码） | P0 |
| 决策界面 | 每条建议含依据 / 动作 / 预期 / 成本，支持「同意」「不同意」+ 补充说明 + 理由标签 | P0 |
| 补充说明优先 | 同意时填写的补充说明**覆盖**原始建议，执行以用户要求为准 | P0 |
| 后台执行 | 08:00 / 21:00 两批次处理决策；`plan` 类写 `plan_patch.json` 由前端应用，`system` 类改代码并 git commit | P1 |
| 撤销窗口 | L1 建议执行后 24h 内可撤销 | P2 |
| 偏好学习 | 同类建议连续拒绝 3 次 → 静默 14 天；沉淀 `evolve_profile.json` | P2 |
| 建议去重 | 同一数据信号 7 天内不重复建议 | P1 |

**质量硬约束**（违反即视为 bug）：每条建议必须① 引用真实数据作为依据；② 动作具体到可直接执行；③ 说明预期效果与验证指标；④ 标注成本。杜绝「建议你多读书」式的空话。

**架构要点**：打卡数据在浏览器 localStorage，自动化 Agent 不可读写。故设计**快照导出 + 补丁回灌**双向通道——前端导出 `checkin_snapshot.json` 供 Agent 读取；Agent 写声明式 `plan_patch.json`，由前端（localStorage 唯一写入方）应用。详见 `设计文档-系统进化模块.md` 第二节。

**明确不做**：AI 直接写 localStorage；自动执行 L2 改动；对话式交互（这是决策界面，不是聊天框）。

> 完整设计（数据结构、接口定义、界面布局、验收标准、分期计划、风险清单）见 `设计文档-系统进化模块.md`。

---

## 五、非功能需求

| 类别 | 需求 |
|---|---|
| **隐私与数据主权** | 全部数据本机存储（localStorage + 本地 JSON + 本地 Obsidian）；个人数据文件 gitignore；唯一外部调用为微信读书 API（用户自有 Key） |
| **可用性** | 三端访问（桌面 App / 浏览器 / 手机 PWA 同局域网）数据互通；PWA 离线可用 |
| **可维护性** | 单文件前端零构建零依赖；`PLAN_SCHEMA` 版本化缓存；备份导出/导入（JSON）一键迁移 |
| **可观测性** | 每日 7:00 自测 + 一致性自愈 + CHANGELOG 强制记录；App 日志 `/tmp/xz_app.log` |
| **性能** | 单文件约 3900 行原生 JS，本地秒开；数据轮询 60s，接口轻量 |
| **容错** | 微信读书空响应防御；网络中断后自动化可手动补跑（有先例：8/27 复盘分析补跑） |

---

## 六、边界与已知限制

### 6.1 已知限制（接受现状，列为风险监控项）

1. **顺延只回看一天**：若整天没打开 App（当日计划未生成），欠账链会断。假设「用户每天使用」，由 7:00 自测兜底告警。
2. **桌面壳路径硬编码**：App 内写死本机 Python / 脚本 / Obsidian 路径，不可分发。
3. **移动端未同步规划模块**：手机端可看可打卡，规划页新模块未适配。
4. **自动化依赖外部调度**：定时任务跑在 Agent 平台，网络/平台故障会中断（已有补跑预案）。

### 6.2 明确不做（Out of Scope）

- ❌ 多用户 / 账号体系 / 云同步（与本地优先原则冲突）
- ❌ 前端框架化、组件库、构建链（与单文件原则冲突）
- ❌ 社交、分享、排行榜（个人系统，无社交诉求）
- ❌ 通用化配置界面（N=1，改配置 = 改代码，与 AI 结对维护）

---

## 七、路线图（Backlog）

| 阶段 | 事项 | 优先级 | 状态 |
|---|---|---|---|
| 已完成 | 进度驱动重构、单一数据源、一致性自愈、微信读书集成、AI 复盘闭环、每日自测 | — | ✅ 2026-08 |
| 短期 | 顺延欠账多日回溯（`planRolloverFrom` 向前查 N 天） | P1 | 待排期 |
| 短期 | 复盘分析中断自动补跑机制 | P1 | 待排期 |
| **短期** | **系统进化模块 P0**：打卡快照导出 + 建议生成 + 决策界面（跑通数据闭环） | **P1** | **设计评审中** |
| 中期 | 系统进化模块 P1：规划补丁自动应用 + 偏好学习 + 采纳率统计 | P2 | 待排期 |
| 中期 | 系统进化模块 P2：`system` 类建议自动编码 + 改动时间线 + git 回滚 | P2 | 待排期 |
| 中期 | 规划模块 P2/P3：周月视图「完成后投影」、AI 节奏建议 | P2 | 设计稿见 `设计文档-时间规划模块.md` |
| 中期 | 移动端规划模块适配 | P2 | 待评估 |
| 长期 | 桌面壳路径配置化，支持分发他人 | P3 | 低优 |

---

## 八、附录：数据口径速查

| 口径 | 单一事实源 | 出口函数 |
|---|---|---|
| 今日该做什么 | `wb_life_plan_daily` | `getTodayDailyTasks()` |
| 读书进度 | `wb_life_reading_cursor` | `getTodayReadingChapter()` |
| 学习进度 | `wb_life_agent_cursor` | `getTodayAgentChapter()` |
| 读书打卡 | `wb_life_plan_ck` | `planSyncTodayFromCheckins()` |
| 学习打卡 | `wb_life_agent_ck` | 同上 |
| 微信读书数据 | `weread_data.json` → `wb_life_weread` | `renderHomeNotes()` 等 |
| 复盘 | `review.json` | 复盘页 + 归档脚本 |
| 打卡快照（供 AI 分析） | localStorage → `checkin_snapshot.json` | `exportCheckinSnapshot()` |
| 每日改进建议 | `suggestions.json` | `renderEvolve()` |
| 用户决策反馈 | `evolve_feedback.json` | `submitDecision()` |
| 规划补丁 | `plan_patch.json` | `applyPlanPatches()` |
| 建议偏好画像 | `evolve_profile.json` | 06:30 建议生成任务 |

> 详细机制（顺延数据流、缓存兜底、维护手册）见 `项目交接文档.md` 第四节。
